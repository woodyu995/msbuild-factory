# Local verify on Intel macOS (Docker Desktop → Linux VM, amd64)
# Usage (repo root):
#   docker compose -f docker-compose.local.yml up --build
#
# Portal is Linux-only (node/python). On Intel Mac this is the native path.
services:
  portal:
    platform: linux/amd64
    build:
      context: .
      dockerfile: portal/Dockerfile
    ports:
      - "8000:8000"
    environment:
      PORTAL_ALLOW_INSECURE_DEFAULTS: "true"
      PORTAL_SIMULATE_WORKERS: "true"
      PORTAL_DEFAULT_ACTOR_ROLES: "operator,builder"
      PORTAL_REQUIRE_AUTH: "false"
      PORTAL_GIT_RESOLVE_MODE: "placeholder"
      PORTAL_DATABASE_URL: "sqlite:////tmp/portal-local.db"
      PORTAL_REGISTRY_HOST: "nexus.company.io"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/readyz')"]
      interval: 10s
      timeout: 3s
      retries: 10
      start_period: 25s
